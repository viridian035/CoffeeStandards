#!/usr/bin/env node

 // Generated by CoffeeScript 2.7.0
(function() {
  // _CONSTATNS
  var CHPR, CP, DEL, EXEC, FCHK, FIN, FOUT, FS, LSDIR, MKDIR, PJN, PTH, STR, WRITE, _DIR_OPTS, _FILE_OPTS;

  _DIR_OPTS = {
    recursive: true
  };

  _FILE_OPTS = {
    encoding: 'utf8'
  };

  // _IMPORTS
  FS = require('fs');

  PTH = require('path');

  CHPR = require('child_process');

  // BASIC
  PJN = function(...parts) {
    return PTH.join(...parts.map(STR));
  };

  STR = function(x) {
    return x.toString();
  };

  WRITE = function(...items) {
    return console.log(...items);
  };

  // CHILD PROCESS
  EXEC = function(cmd) {
    var error;
    try {
      return CHPR.execSync(cmd);
    } catch (error1) {
      error = error1;
      return WRITE('error running ' + cmd);
    }
  };

  // FS
  CP = function(src, dst) {
    return FS.cpSync(src, dst, _DIR_OPTS);
  };

  FCHK = function(pth) {
    return FS.existsSync(pth);
  };

  FIN = function(pth) {
    return FS.readFileSync(pth, _FILE_OPTS);
  };

  FOUT = function(pth, cnt) {
    return FS.writeFileSync(pth, cnt, _FILE_OPTS);
  };

  LSDIR = function(pth) {
    return FS.readdirSync(pth, _DIR_OPTS);
  };

  MKDIR = function(pth) {
    return FS.mkdirSync(pth, _DIR_OPTS);
  };

  DEL = function(pth) {
    return FS.rmSync(pth, _DIR_OPTS);
  };

  // _EXPORT
  module.exports = {PJN, STR, WRITE, EXEC, FCHK, FIN, FOUT, LSDIR, MKDIR, DEL};

}).call(this);
